<%= simple_form_for([ restaurant, review ], remote: true) do |form| %>
  <%= form.input :content, label: 'What did you think?', as: :text %>
  <%= form.button :submit, class: 'btn btn-primary' %>
<% end %>